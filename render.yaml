services:
  - type: web
    name: mi-backend
    rootDir: app/back
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    plan: free

#Listo para Render